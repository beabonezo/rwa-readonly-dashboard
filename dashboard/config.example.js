// config.example.js — Template de configuração local
// 1. Copie este arquivo para config.js (que está no .gitignore)
// 2. Substitua os valores abaixo pelos reais
// 3. config.js NÃO deve ser commitado

export const PROJECT_ID = 'SEU_PROJECT_ID_AQUI';
// Obtenha um Project ID gratuito em: https://cloud.walletconnect.com
// Sem este ID, o WalletConnect (QR Code) não vai funcionar.
// MetaMask e Rabby via extensão do browser funcionam sem configuração.
